<h1>Become a doctor at our hospital</h1>

<%= form_with(model: @doctor, local: true, class: "ui form") do |f| %>
<div class="field">
  <%= f.label :name %>
  <%= f.text_field :name, required: true, placeholder: "Doctor Name" %>
</div>
<%= f.submit  %>
<% end %>
